import React, { Component, PropTypes } from 'react';
import classNames from 'classnames';

class Palette extends Component {
  render() {
    return <h3>Im a Palette!</h3>;
  }
}

export default Palette;
